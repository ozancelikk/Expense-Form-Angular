export interface ColDef{
    editable:boolean
    sortable: boolean
    minWidth:number
    filter: boolean
    resizable: boolean
    floatingFilter: boolean
    flex: number
    rowSelection:string
}