import { SystemLogPipePipe } from './system-log-pipe.pipe';

describe('SystemLogPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SystemLogPipePipe();
    expect(pipe).toBeTruthy();
  });
});
